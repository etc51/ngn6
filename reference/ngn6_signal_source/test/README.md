# reference/ngn6_signal_source/test

## Purpose
Legacy Node.js tests for the reference signal source.

## Contents
- Tests for config, heartbeat, news feed, paper trader, signal engine, signal watcher, and auto context.

## Rules
- These tests validate reference behavior only.
- Python bot changes should be tested in project-level `tests/`.
- Keep any new Node tests deterministic and isolated from live credentials.

## Quick Checks
- Run from `reference/ngn6_signal_source` with the package's Node test command.
